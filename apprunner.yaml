version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt

run:
  runtime-version: 3.11
  pre-run:
    - pip install --upgrade pip
    - pip install -r requirements.txt
  command: python -m uvicorn app.app:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
